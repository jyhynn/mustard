<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.ReplyMapper">

	<insert id="insertReply">
		insert into reply values(seq_reply.nextval,#{board_no},#{notice_no},#{reply},#{writer},sysdate,#{memNo})
	</insert>
	
	<select id="get" resultType="com.spring.domain.ReplyVO">
		select * from reply where board_no=#{board_no} and notice_no=#{notice_no}
	</select>
	
	<delete id="delete"> 
		delete from reply where board_no=#{board_no} and notice_no=#{notice_no} and reNo=#{reNo}
	</delete>
	
	<select id="selected" resultType="com.spring.domain.ReplyVO">
		select * from reply where board_no=#{board_no} and notice_no=#{notice_no} and reNo=#{reNo}
	</select>
	
	<select id="replyCount">
		select count(*) from reply where board_no=#{board_no} and notice_no=#{notice_no}
	</select>
</mapper>